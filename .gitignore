_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor

# Resolved per-machine: the local Ruby here is much older than CI's.
Gemfile.lock

# Abstract cache for script/sync_publications.py
script/.publications_cache.json
